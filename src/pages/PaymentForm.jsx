// src/pages/PaymentForm.jsx
import PaymentFormComponent from '@/components/payments/PaymentForm';

const PaymentForm = ({ isEdit }) => {
  return <PaymentFormComponent isEdit={isEdit} />;
};

export default PaymentForm;